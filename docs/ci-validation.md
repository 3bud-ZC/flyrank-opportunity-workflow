# CI validation

This branch exists only to run the repository test and production-build workflow through a pull request before the Week 4 submission is finalized.

Second run: verifies the workflow after removing the lockfile-dependent npm cache configuration.

Third run: verifies the Vite asset declarations and complete production build.
